import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF4263EB); // Modern blue
  static const Color primaryLight = Color(0xFFEEF1FF);
  static const Color primaryDark = Color(0xFF2C4BE0);

  // Background colors
  static const Color background = Color(0xFFF5F7FA);
  static const Color cardBackground = Colors.white;

  // Text colors
  static const Color textPrimary = Color(0xFF1A1D1F);
  static const Color textSecondary = Color(0xFF6F767E);
  static const Color textLight = Color(0xFF9A9FA5);

  // Accent colors
  static const Color accent = Color(0xFF38BDF8); // Blue accent
  static const Color success = Color(0xFF22C55E); // Green for success
  static const Color warning = Color(0xFFF59E0B); // Amber for warnings
  static const Color error = Color(0xFFEF4444); // Red for errors

  // Moroccan-inspired accent colors
  static const Color moroccanRed = Color(0xFFE63946);
  static const Color moroccanBlue = Color(0xFF457B9D);
  static const Color moroccanGreen = Color(0xFF2A9D8F);
  static const Color moroccanYellow = Color(0xFFF4A261);

  // Gradient colors
  static const List<Color> blueGradient = [
    Color(0xFF4263EB),
    Color(0xFF5E7BF7),
  ];

  // UI element colors
  static const Color divider = Color(0xFFEAEEF2);
  static const Color shadow = Color(0x0D000000);
  static const Color shimmer = Color(0xFFEEF1FF);

  // Additional UI colors
  static const Color card = Colors.white;
  static const Color cardShadow = Color(0x0D000000);
  static const Color tagBackground = Color(0xFFF5F7FA);
  static const Color activeTab = Color(0xFF4263EB);
  static const Color inactiveTab = Color(0xFF9A9FA5);
}
